import { CommandInterface } from '../interface/command.interface';
import BuildingsCommand from './buildings-requirements.command';

const BuildingsCommands: CommandInterface[] = [BuildingsCommand];

export default BuildingsCommands;

